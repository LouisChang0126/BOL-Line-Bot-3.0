# 測試項目

## 核心功能
1. 能新增服事、新增週次後，刷新頁面後資料還在
2. 能移除服事、移除週次後，刷新頁面後資料還在
3. 能拖拉服事人員、編輯服事人員後，刷新頁面後資料還在
4. 能剪下貼上、複製貼上、刪除後，刷新頁面後資料還在
5. 能用Agent輸入csv來編輯排班後，接受後，刷新頁面後資料還在
6. 按下編輯顯示欄位、查看編輯紀錄、管理使用者後有跳出modal或是跳轉視窗
7. Agent排班正常

## 額外測試項目（Vue 重構版新增，建議納入）
8. 撤銷/重做（Ctrl+Z / Ctrl+Y、工具列按鈕）狀態正確
9. 改名服事項目後，刷新仍在，且 displayConfig / 歷史班表 / users.serve_types 同步更新
10. 資訊欄位（不含人名，如「彩排」）新增/編輯/刪除後，刷新仍在
11. 編輯顯示欄位：群組拖拉、隱藏項目，儲存後公開查看頁(/view)分組正確
12. 匯出 Excel 能下載且內容正確
13. 單一編輯分頁鎖：開第二個分頁時，第一個分頁出現「已在其他分頁編輯」遮罩
14. 公開查看頁(/view?service=&user=)：顯示班表、可展開歷史週、?user= 對應人名有標示
15. 管理端權限：未登入→導向 /admin/login；已登入但不在 _config/admins→顯示「無管理權限」
16. 公開首頁(/)：無 ?user= 顯示大眾 Landing；有 ?user= 顯示個人班表選擇

## 自動化測試結果（Playwright，2026-06-07，對 global-serve-system）
- ✅ 1, 2, 3, 4, 6 全數通過（含每步「刷新後資料還在」）
- ✅ 5 Agent 匯入 CSV → 全部接受 → 刷新後資料仍在（CSV 內人名如「劉婕」出現）
- ✅ 7 Agent 排班：對空白週 2026.07.19 產生 14 筆指派 → 接受 → 刷新後仍在
- ✅ 8（撤銷/重做）、9（改名服事 + displayConfig/歷史/users 同步）通過
- ✅ 10 資訊欄位、11 顯示分組(隱藏項目→公開頁不顯示)、12 匯出 Excel、
  13 分頁鎖、14 公開查看頁+人名標示、15 權限閘門(未登入導向登入頁)、16 首頁分派 全數通過
- 結論：**1–16 全數通過**（自動化腳本見 `__playwright_test/run_*_tests.mjs`）

> 修正記錄：Agent 一開始回 `Failed to fetch`，原因是 AI 後端 Cloud Run
> `bol-scheduler-agnet` 的 CORS 允許清單未含新網域。已於 `agentConfig.py` 補上
> `global-serve-system.web.app` / `localhost:5173` 並重新部署該服務 → 5, 7 通過。

> 自動化腳本：`__playwright_test/run_core_tests.mjs`、`run_agent_tests.mjs`
> （透過 CDP attach 到使用者已登入的 Edge，避免 Google 擋自動化瀏覽器登入）
